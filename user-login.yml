server:
  port: "8082"
spring:
  application:
    name: user-login
  datasource:
    url: jdbc:mysql://172.23.239.124:3306/authentication?useSSL=false
    username: "root"
    password: ""
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
  kafka:
    bootstrap-servers: 172.23.239.49:9092
eureka:
  client:
    register-with-eureka: "true"
    fetch-registry: "true"
    serviceUrl:
      defaultZone: http://172.23.239.100:8761/eureka
management:
  endpoints:
    web:
      exposure:
        include: '*'

