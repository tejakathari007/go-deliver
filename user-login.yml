server:
  port: "8082"
spring:
  application:
    name: user-login
  datasource:
    url: jdbc:mysql://172.23.239.124:3306/go_d?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.MySQL5Dialect
    generate-ddl: "true"
    show-sql: "true"
eureka:
  client:
    register-with-eureka: "true"
    fetch-registry: "true"
    serviceUrl:
      defaultZone: http://172.23.239.100:8761/eureka
management:
  endpoints:
    web:
      exposure:
        include: '*'

